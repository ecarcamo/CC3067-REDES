/** Algoritmo de correccion Hamming generico (n, m). Implementacion pendiente (Hito 1). */

import { AlgoritmoIntegridad } from "./contrato.js";

export class Hamming extends AlgoritmoIntegridad {
  nombre = "hamming";

  calcular(bits) {
    throw new Error("no implementado");
  }

  verificar(trama, parametros) {
    throw new Error("no implementado");
  }
}
